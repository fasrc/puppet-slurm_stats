#!/bin/bash

/usr/bin/date -d "yesterday" '+%B %d' > <%= @data_store %>/fairshare.txt
/usr/bin/sshare -n --parsable2 -a -o "User,Account,Fairshare" | sort | tr -d ' ' >> <%= @data_store %>/fairshare.txt
